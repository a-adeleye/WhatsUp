import * as React from "react";
import "./SectionSpacer.scss";

const SectionSpacer: React.FC = () => {
    return <div className={"section-spacer"} aria-hidden={"true"} />;
};

export default SectionSpacer;
